1. docker compose is taking environment variable value when it should not in goals-app-noargs
2. port exposing and mapping issue in docker compose of goals-app-noargs
3. bad shutdown of container in docker compose of goals-app-noargs
